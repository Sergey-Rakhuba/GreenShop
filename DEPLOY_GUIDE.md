# 📝 Покрокова інструкція публікації GreenShop на Render.com

## 🎯 Що потрібно перед початком

- ✅ Акаунт на [GitHub](https://github.com) (безкоштовно)
- ✅ Акаунт на [Render.com](https://render.com) (безкоштовно)
- ✅ Git встановлений на вашому комп'ютері

---

## 📋 Крок 1: Створення репозиторію на GitHub

### 1.1 Створіть новий репозиторій

1. Відкрийте [GitHub](https://github.com)
2. Натисніть **"+"** → **"New repository"**
3. Заповніть:
   - **Repository name:** `greenshop`
   - **Description:** `GreenShop - Інтернет-магазин рослин на PHP`
   - **Visibility:** Public (публічний)
   - **НЕ ВМИКАЙТЕ** "Add a README file"
4. Натисніть **"Create repository"**

### 1.2 Скопіюйте URL репозиторію

GitHub покаже щось типу:
```
https://github.com/your-username/greenshop.git
```

---

## 🔧 Крок 2: Завантаження коду на GitHub

### 2.1 Відкрийте PowerShell у папці проекту

```powershell
cd C:\prog_php\GreenShop
```

### 2.2 Ініціалізуйте Git

```powershell
git init
```

### 2.3 Додайте всі файли

```powershell
git add .
```

### 2.4 Зробіть перший коміт

```powershell
git commit -m "Initial commit: GreenShop e-commerce project"
```

### 2.5 Додайте віддалений репозиторій

**ЗАМІНІТЬ `your-username` на ваш GitHub username!**

```powershell
git remote add origin https://github.com/your-username/greenshop.git
```

### 2.6 Перевірте гілку та перейменуйте на main (якщо потрібно)

```powershell
# Перевірка поточної гілки
git branch

# Якщо гілка називається master, перейменуйте на main
git branch -M main
```

### 2.7 Відправте код на GitHub

```powershell
git push -u origin main
```

**Можливо знадобиться авторизація:**
- GitHub попросить ввести username та пароль
- Якщо у вас двофакторна авторизація, використовуйте **Personal Access Token** замість пароля

**Як створити Personal Access Token:**
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token → Classic
3. Виберіть: `repo` (всі галочки)
4. Generate token
5. Скопіюйте токен і використовуйте замість пароля

---

## 🚀 Крок 3: Деплой на Render.com

### 3.1 Реєстрація на Render

1. Відкрийте [render.com](https://render.com)
2. Натисніть **"Get Started"**
3. **Рекомендовано:** Виберіть "Sign up with GitHub" (швидше і зручніше)
4. Підтвердіть доступ до GitHub

### 3.2 Створення Web Service

1. На дашборді Render натисніть **"New +"** (праворуч вгорі)
2. Виберіть **"Web Service"**

### 3.3 Підключення репозиторію

1. У списку репозиторіїв знайдіть **greenshop**
2. Натисніть **"Connect"**

**Якщо не бачите репозиторій:**
- Натисніть "Configure account" 
- Надайте Render доступ до вашого репозиторію

### 3.4 Налаштування сервісу

Заповніть форму:

| Поле | Значення |
|------|----------|
| **Name** | `greenshop` (або будь-яка інша назва) |
| **Region** | `Frankfurt (EU Central)` (ближче до України) |
| **Branch** | `main` |
| **Root Directory** | (залишіть пустим) |
| **Environment** | `PHP` |
| **Build Command** | `composer install --no-dev --optimize-autoloader || true` |
| **Start Command** | `php -S 0.0.0.0:$PORT -t .` |

### 3.5 Виберіть план

- **Plan:** `Free` (0$/місяць)
- **Instance Type:** `Free` (512 MB RAM)

### 3.6 Створення сервісу

1. Натисніть **"Create Web Service"** (внизу сторінки)
2. Render почне деплой - це займе 2-5 хвилин

### 3.7 Моніторинг деплою

Ви побачите лог з процесом деплою:
```
==> Cloning from https://github.com/your-username/greenshop...
==> Checking out commit...
==> Running build command...
==> Starting service...
==> Service running
```

---

## ✅ Крок 4: Перевірка сайту

### 4.1 Отримайте URL

Після завершення деплою Render надасть вам URL:
```
https://greenshop.onrender.com
```

або

```
https://greenshop-xxxx.onrender.com
```

### 4.2 Відкрийте сайт

1. Натисніть на URL у Render
2. Або скопіюйте і відкрийте у браузері

⏱️ **Перший запуск може зайняти 30-60 секунд** (безкоштовний план засинає після неактивності)

### 4.3 Перевірте функціонал

✅ Головна сторінка відкривається  
✅ Сторінка магазину з товарами  
✅ Фільтри працюють  
✅ Кошик працює  
✅ Авторизація працює (admin/admin123)  
✅ Адмін-панель доступна  

---

## 🔄 Крок 5: Оновлення сайту (після змін)

Коли ви змінюєте код локально:

```powershell
# 1. Перейдіть у папку проекту
cd C:\prog_php\GreenShop

# 2. Додайте змінені файли
git add .

# 3. Зробіть коміт зі описом змін
git commit -m "Опис ваших змін"

# 4. Відправте на GitHub
git push

# 5. Render автоматично оновить сайт (за ~2 хв)
```

---

## 🎨 Крок 6: Додатково (опціонально)

### Додати власний домен

1. У Render → Ваш сервіс → Settings → Custom Domain
2. Додайте ваш домен
3. Налаштуйте DNS записи у вашого реєстратора

### Увімкнути HTTPS

✅ HTTPS вже увімкнений автоматично на Render!

### Переглянути логи

1. Render → Ваш сервіс → Logs
2. Побачите всі запити та помилки

---

## ❗ Поширені помилки та рішення

### Помилка: "Failed to fetch repository"
**Рішення:** Надайте Render повний доступ до GitHub репозиторію

### Помилка: "Build failed"
**Рішення:** Перевірте, чи є файли `composer.json` та `render.yaml` у репозиторії

### Помилка: "Application error" або 500
**Рішення:** 
- Перегляньте логи у Render
- Перевірте права на запис у папку `config/`

### Сайт довго завантажується
**Рішення:** Це нормально для безкоштовного плану. Render засинає після 15 хв неактивності.

### Зображення не відображаються
**Рішення:** 
- Завантажте зображення у папку `assets/images/home_plants/`
- Або використовуйте заглушки (вони працюють автоматично)

---

## 📊 Обмеження безкоштовного плану Render

✅ **Що є:**
- 512 MB RAM
- Безлімітний трафік
- Безкоштовний SSL (HTTPS)
- Автоматичні деплої з GitHub
- 750 годин роботи на місяць

⚠️ **Обмеження:**
- Сервіс засинає після 15 хвилин неактивності
- Перший запуск після сну: 30-60 секунд
- Немає постійного сховища файлів (data.json може скидатися при рестарті)

---

## 💡 Поради

1. **Backup data.json:** Періодично завантажуйте `data.json` через FTP/SFTP
2. **Моніторинг:** Додайте UptimeRobot для автоматичних пінгів (запобігає засинанню)
3. **GitHub Actions:** Налаштуйте CI/CD для автоматичного тестування
4. **Environment Variables:** Використовуйте змінні середовища для чутливих даних

---

## 🎉 Готово!

Ваш сайт **GreenShop** тепер доступний онлайн! 🌍

Поділіться посиланням з друзями:
```
https://greenshop.onrender.com
```

---

## 📞 Підтримка

- **Render Docs:** [docs.render.com](https://docs.render.com)
- **Render Community:** [community.render.com](https://community.render.com)
- **GitHub Issues:** Створіть issue у вашому репозиторії

---

**Успіхів з вашим онлайн-магазином! 🌱✨**
